# --- Frontend (React App) ---
# The base URL for the API endpoint used by the React app.
# In development, this points to your local n8n instance.
# REACT_APP_API_BASE_URL="http://localhost:5678/webhook"

# A key used for authentication or API access by the frontend.
# REACT_APP_API_KEY=your_frontend_api_key_here

# --- Backend (n8n) ---
# The time zone for the n8n instance. Use "America/Bogota" for Colombia.
# TZ=

# Basic authentication credentials to protect your n8n instance.
# N8N_BASIC_AUTH_ACTIVE=true
# N8N_BASIC_AUTH_USER=
# N8N_BASIC_AUTH_PASSWORD=

# A secure key for encrypting credentials within n8n.
# N8N_ENCRYPTION_KEY=

# Host and port for the n8n instance.
# N8N_HOST=localhost
# N8N_PORT=5678
# N8N_PROTOCOL=http

# --- Database (Supabase) ---
# Credentials for the Supabase database.
# SUPABASE_PROJECT_ID=your_supabase_project_id
# SUPABASE_API_KEY=your_supabase_api_key
# SUPABASE_SERVICE_ROLE=your_supabase_service_role